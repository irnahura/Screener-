# screener — AI that knows everything you've seen, said, or heard
# https://screener.com

# Leaf crate on purpose: runtime CPU-feature detection is needed by both
# screener-core AND screener-config, and config cannot depend on core
# (cycle: core → screener-secrets → config). Zero dependencies — keep it
# that way so it can sit below everything.
[package]
name = "screener-cpu-features"
version.workspace = true
authors.workspace = true
description = "Runtime CPU feature detection (AVX2 gating) for screener"
repository.workspace = true
license-file.workspace = true
edition.workspace = true

[dependencies]
